## Disable monitoring of logstash to remove local elastic dependency
XPACK_MONITORING_ENABLED=false

##Environment variables generated by ansible
{% for var,val in pipeline_ingest.logstash_env.items() %}
{{var}}={{val}}
{% endfor %}
